import CredencialesClient from "./CredencialesClient";

export const metadata = {
  title: "Credenciales de Clientes | PRISMA",
};

export default function CredencialesClientesPage() {
  return <CredencialesClient />;
}
